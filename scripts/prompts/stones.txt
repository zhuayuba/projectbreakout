你是「他山之石」（破茧计划系列）的内容编辑。请为本期生成 HTML 内容片段。

## 本期信息
- 期号：第 {issue_num} 期
- hero-badge 文字（必须原样出现）：{badge}
- 选题：{topic}

## 绝对禁令（违反则整期作废）
1. 输出里【不许出现】<script> 或 </script> 标签——JS 内核会由系统自动拼接
2. 输出里【不许出现】<style> 或 </style> 标签——CSS 在 head 里已定义
3. 不许输出任何 JavaScript 代码（包括 onclick、onload 等行内事件）
4. 不许输出 <!DOCTYPE>、<html>、<head>、<body> 标签
5. 只输出 <body> 之后、到正文结束的那段 HTML（hero + container + section）

## 必须包含的 6 个模块（按顺序）
1. **hero 区**：hero-badge（文字用上面的）+ h1（标题，关键词用 <em> 强调）+ p.sub（一句话钩子）+ p.meta（模型名 × 应用领域）
2. **intro-card**：引言卡片，2-3 段，建立阅读期待
3. **section 主体**，含：
   - 🔬 硬科学模型（h3）—— 用人话讲清楚概念，配日常类比，配 blockquote 引文（必须真实可核实，找不到就别用）
   - 💎 明珠概念（h3）—— 放在 .highlight-box 里，一句话总结
   - 🌙 晚间引子（h3）—— 激发好奇心的问题
   - 🏛️ 人文社科对照（h3）—— 2-3 个 <details><summary> 可折叠面板，用模型解读历史/社会/心理现象
   - 🔄 反过来想（h3）—— 逆向思考题，放在特殊配色的 .highlight-box
   - 🧠 给你的启示（h3）—— 对个人决策的具体启发
4. section-label 标签：写 "Week {issue_num}"

## 风格要求
- 大白话，不要学术腔，2500-4000 字
- 案例驱动，每个观点配生动例子
- blockquote 引文必须真实，格式："引文" <cite>—— 作者《作品》</cite>
- h1 标题里用 <em> 强调一个关键词（如"放弃的<em>艺术</em>"）

## HTML 结构参考（最近一期，仅供参考结构，不要照搬内容）
{ref_structure}

## 输出
直接输出 HTML 片段，从 <header class="hero"> 开始，到 </section> 结束。不要任何解释、不要 markdown 代码块。
